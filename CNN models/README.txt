This folder contains the CNN models used for testing. 
The models are made by the CNNmodels.py class and are atored in this folder until called by the mainController.py class
if this folder is removed, the references wont work and it will cause the application to crash. 
